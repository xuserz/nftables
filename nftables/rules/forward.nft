chain forward {
  type filter hook forward priority 0; policy accept;

  # TCP MSS clamping (важно для VPN)
  tcp flags syn tcp option maxseg size set rt mtu

  # Блокировка NNN с сервера
  ip daddr $block_ipv4 drop
  ip6 daddr $block_ipv6 drop
}